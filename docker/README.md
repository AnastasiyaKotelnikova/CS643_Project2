# Docker

This folder contains files for containerizing training or inference code.

Files to be added:
- Dockerfile – container setup for model inference or training
- docker-compose.yml – optional, if needed
- app.py – Flask or FastAPI wrapper for inference (if API is used)

Purpose:
Build and push Docker container to Docker Hub for model deployment.
